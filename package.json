{
    "name": "memviz-workspace",
    "workspaces": [
        "memviz-glue",
        "extension"
    ],
    "scripts": {
        "package": "npm run build --prefix memviz-glue && npm run package --prefix extension"
    }
}